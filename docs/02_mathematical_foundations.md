# Kaleidoscope: Mathematical Foundations

---

## 1. Overview

This document defines the mathematical frameworks underpinning the Kaleidoscope pattern generation system. These formulas provide:
1. **Infinite generative capacity** through combinatorial prompt construction
2. **Rigorous symmetry** via group theory (wallpaper groups)
3. **Organic complexity** through fractal mathematics
4. **Legal basis** for human authorship claims on transformed outputs

---

## 2. Prompt Generation Mathematics

### 2.1 Combinatorial Word Selection

Given:
- `L` = Number of word lists (default: 20)
- `W` = Words per list (default: 50)
- `k` = Words selected per prompt (variable: 1-100)

**Total unique word combinations:**

```
C(total) = Σ(k=1 to 100) [ Π(i=1 to L) C(W, k_i) ]
         where Σ k_i = k
```

**Simplified lower bound (selecting 1 word from each list):**

```
C_min = W^L = 50^20 ≈ 9.5 × 10^33 unique combinations
```

### 2.2 Prompt Temperature Variation

For LLM prompt generation with temperature `T`:
- `T = 0.0`: Deterministic (same output for same input)
- `T = 1.0+`: Maximum creativity/variation

**Effective uniqueness multiplier:**

```
U(T) = 1 + (T × V_base)
```

Where `V_base` ≈ 100 (empirical variation factor for T=1.0)

---

## 3. Wallpaper Group Symmetry

### 3.1 The 17 Wallpaper Groups

All possible 2D repeating patterns are classified into exactly 17 symmetry groups:

| Group | Rotation | Reflection | Glide | Lattice |
|-------|----------|------------|-------|---------|
| p1 | - | - | - | Oblique |
| p2 | 180° | - | - | Oblique |
| pm | - | Yes | - | Rectangular |
| pg | - | - | Yes | Rectangular |
| cm | - | Yes | Yes | Rhombic |
| pmm | 180° | Yes | - | Rectangular |
| pmg | 180° | Yes | Yes | Rectangular |
| pgg | 180° | - | Yes | Rectangular |
| cmm | 180° | Yes | Yes | Rhombic |
| p4 | 90° | - | - | Square |
| p4m | 90° | Yes (2 axes) | - | Square |
| p4g | 90° | Yes | Yes | Square |
| p3 | 120° | - | - | Hexagonal |
| p3m1 | 120° | Yes (⟂ to sides) | - | Hexagonal |
| p31m | 120° | Yes (∥ to sides) | - | Hexagonal |
| p6 | 60° | - | - | Hexagonal |
| p6m | 60° | Yes | - | Hexagonal |

### 3.2 Transformation Matrices

**Rotation by angle θ:**
```
R(θ) = | cos(θ)  -sin(θ) |
       | sin(θ)   cos(θ) |
```

**Reflection across line through origin at angle α:**
```
M(α) = | cos(2α)   sin(2α) |
       | sin(2α)  -cos(2α) |
```

**Translation by vector (a, b):**
```
T(a,b): (x, y) → (x + a, y + b)
```

**Glide reflection (reflection + translation):**
```
G(α, d) = T(d·cos(α), d·sin(α)) ∘ M(α)
```

### 3.3 Fundamental Domain to Full Pattern

For a fundamental domain `D` and symmetry group `G`:

```
Pattern P = ⋃(g ∈ G) g(D)
```

Where `G` is the infinite group generated by the symmetry operations.

---

## 4. Kaleidoscope Mathematics

### 4.1 N-Fold Mirror Reflections

For an N-fold kaleidoscope centered at origin:

**Mirror angles:**
```
θ_k = πk/N    for k = 0, 1, 2, ..., N-1
```

**Reflection transformation in complex plane:**
```
z' = e^(2iθ_k) · z̄
```

Where `z̄` is the complex conjugate.

### 4.2 Dihedral Group D_N

The kaleidoscope generates the dihedral group:
- `N` rotations: `r^k` for k = 0..N-1
- `N` reflections: `s·r^k` for k = 0..N-1

**Total elements:** `|D_N| = 2N`

### 4.3 Wedge to Full Pattern

Given a wedge `W` subtending angle `π/N`:

```
Full Pattern = ⋃(k=0 to 2N-1) D_N^k(W)
```

### 4.4 Radial Mapping

**Polar to Cartesian:**
```
x = r·cos(θ)
y = r·sin(θ)
```

**Spiral distortion:**
```
r' = r + a·θ    (Archimedean spiral)
r' = r·e^(bθ)   (Logarithmic spiral)
```

---

## 5. Fractal Mathematics

### 5.1 Mandelbrot Set

**Iteration formula:**
```
z_{n+1} = z_n² + c
z_0 = 0
```

**Membership criterion:**
```
c ∈ M ⟺ lim(n→∞) |z_n| < ∞
```

**Practical escape test:**
```
|z_n| > 2 → c ∉ M
```

### 5.2 Julia Sets

**For fixed parameter c:**
```
z_{n+1} = z_n² + c
z_0 = pixel coordinate
```

**Filled Julia set K_c:**
```
K_c = { z_0 : lim(n→∞) |z_n| < ∞ }
```

### 5.3 L-Systems (Lindenmayer Systems)

**Formal definition:**
```
G = (V, ω, P)
```
Where:
- `V` = Alphabet of symbols
- `ω` = Axiom (initial string)
- `P` = Production rules

**Example: Koch Snowflake**
```
V = {F, +, -}
ω = F
P: F → F+F--F+F

Interpretation:
  F = draw forward
  + = turn left 60°
  - = turn right 60°
```

**Iteration produces:**
```
n=0: F
n=1: F+F--F+F
n=2: F+F--F+F+F+F--F+F--F+F--F+F+F+F--F+F
...
```

### 5.4 Fractal Dimension

**Box-counting dimension:**
```
D = lim(ε→0) [ log(N(ε)) / log(1/ε) ]
```

Where `N(ε)` = number of boxes of size `ε` needed to cover the fractal.

---

## 6. Tiling Mathematics

### 6.1 Regular Tiling

**Only 3 regular tilings of the plane:**
- Triangles (3.3.3.3.3.3)
- Squares (4.4.4.4)
- Hexagons (6.6.6)

**Vertex condition: Σ(1/n_i) = 2 for meeting polygons**

### 6.2 Penrose Tiling (Aperiodic)

**Two rhombi with angles:**
- Thin rhombus: 36°, 144°
- Thick rhombus: 72°, 108°

**Matching rules enforce aperiodicity.**

**Ratio of thick:thin rhombi = φ (golden ratio)**

### 6.3 Wang Tiles

**Tile set T with edge colors:**
- Edges must match adjacent tile edges
- Infinite plane coverage determination is undecidable
- Can simulate any Turing machine

---

## 7. Seamless Tiling Algorithm

### 7.1 Edge Matching

For image `I` of size `W × H`:

**Horizontal wrap condition:**
```
I(0, y) ≈ I(W-1, y)  ∀y ∈ [0, H)
```

**Vertical wrap condition:**
```
I(x, 0) ≈ I(x, H-1)  ∀x ∈ [0, W)
```

### 7.2 Gradient Domain Blending

**Poisson equation for seamless stitching:**
```
∇²f = ∇²g   (interior)
f = f*      (boundary)
```

Solve via discrete Laplacian operator.

### 7.3 Fourier-Based Tiling

**Mirror padding eliminate edge discontinuities:**
```
I_tiled(x, y) = I(|x mod 2W - W|, |y mod 2H - H|)
```

---

## 8. Color Space Mathematics

### 8.1 HSL/HSV Transformations

**RGB to HSL:**
```
H = arctan2(√3(G-B), 2R-G-B)
S = (max - min) / (1 - |2L - 1|)
L = (max + min) / 2
```

### 8.2 Palette Generation

**Complementary:**
```
H_comp = (H + 180°) mod 360°
```

**Triadic:**
```
H_1 = H, H_2 = (H + 120°) mod 360°, H_3 = (H + 240°) mod 360°
```

**Analogous:**
```
H_i = (H + i × 30°) mod 360° for i = -2..2
```

---

## 9. Provenance Hash Function

**For IP documentation, each pattern includes:**

```
Hash = SHA-256(
    word_seeds ||
    prompt_text ||
    ai_model_version ||
    transform_params ||
    timestamp
)
```

**This creates verifiable chain of creation enabling IP claims.**

---

## 10. Pattern Uniqueness Estimation

**For full system capacity:**

```
Unique Patterns = 
    Word Combinations      × 
    AI Stochasticity       × 
    Transform Variations   × 
    Color Palette Options

= (50^20) × (100) × (17 × 12 × 10) × (10^6)
≈ 10^42 unique patterns
```

**Far exceeding any practical generation capacity.**

---

*Mathematical Foundations v1.0 | Authored as human-created IP documentation*
